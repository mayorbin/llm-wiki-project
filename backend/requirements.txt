# LLM Wiki 后端依赖
# 安装: pip install -r requirements.txt

# 核心框架
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# 数据模型与配置
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0

# LLM 调用
litellm>=1.50,<2.0

# 知识图谱
networkx>=3.0,<4.0

# 文件转换
markitdown>=0.1.5,<1.0

# 认证
python-jose[cryptography]>=3.3,<4.0
bcrypt>=4.0,<6.0

# 并发锁
filelock>=3.13,<4.0

# 文件上传
python-multipart>=0.0.9,<1.0

# 配置文件解析
pyyaml>=6.0,<7.0

# 数据库迁移
alembic>=1.13,<2.0

# ── 开发依赖 ──
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0
ruff>=0.4,<1.0
